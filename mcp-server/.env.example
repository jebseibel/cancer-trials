# Base URL of the running Spring Boot backend (no trailing slash).
CANCER_API_BASE_URL=http://localhost:8080

# A long-lived JWT obtained by logging in as `jeb` via POST /api/auth/login.
# First-pass auth: one static token, sent as Authorization: Bearer on every call.
CANCER_API_TOKEN=
